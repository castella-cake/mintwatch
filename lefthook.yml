min_version: 1.3.0
pre-commit:
    parallel: true
    commands:
        eslint:
            glob: '*.{js,jsx,ts,tsx}'
            run: pnpm exec eslint {staged_files} --ignore-path .gitignore --fix
            stage_fixed: true
            skip:
                - merge
                - rebase